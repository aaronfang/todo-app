# SO-Character 任务模板（草稿）

> 来源：现有任务 `jade000新角色制作 (易嘉易)` 的子任务顺序。
> 说明：`jane409...`、`griffith405...` 等已完成任务看起来是角色专项修改项，暂不纳入每个新角色都重复创建的固定模板。

## 使用方式

先创建主任务，再在主任务上点击右键，从“应用模板”中选择本模板。

模板不会在创建主任务时自动套用，也不会自动修改已有任务。

## 主任务示例

`SO｜角色名｜新角色制作`

## 自动创建的子任务

请按实际流程修改、删除或补充：

1. 确认CP
2. 准备素材 + 评估时间
3. 提需求单
4. 中模
5. 高模
6. 低模
7. UV
8. 法线贴图
9. 贴图
10. 顶点色
11. LOD
12. 发绑定
13. 局外录屏
14. 发布局外
15. 解算单 - 局外
16. 创建实体
17. 局内跑测录屏
18. 解算单 - 局内

## 待确认事项

- [ ] 是否保留 `高模`
- [ ] 是否保留 `低模`
- [ ] 是否保留 `UV贴图`
- [ ] 是否保留 `LOD`
- [ ] 是否保留 `引擎调整`
- [ ] 是否需要加入角色专项检查项
- [ ] 是否需要区分必做项和可选项
- [x] 模板通过右键菜单手动应用
- [x] 模板修改不会自动更新已有任务
